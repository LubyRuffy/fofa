<%
if @errmsg
    @error = true
    @message = @errmsg
else
    @error = false
    @message = "更新成功！"
end %>
<%= render 'refresh' %>
<%= render 'resetmodal' %>

$("#logPanel").html("<%= escape_javascript(render 'notice') %>");