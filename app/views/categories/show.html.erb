<h1>规则组详情</h1>
<div class="bs-component">
  <ul class="breadcrumb">
    <li><%= link_to '控制面板', my_path %></li>
    <li><%= link_to '规则组管理', categories_path %></li>
    <li class="active">规则组详情</li>
  </ul>
</div>

<p>
  <strong>名称:</strong>
  <%= @category.title %>
</p>

<p>
  <strong>规则列表:</strong>
  <% @category.rules.each{|r| %>
      <%= raw('<li>') + link_to(r.product, r) + raw('</li>') %>
  <% } %>
</p>

<%= link_to '编辑', edit_category_path(@category) %>
